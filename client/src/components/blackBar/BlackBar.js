import React from 'react';
import { Container } from 'react-bootstrap';
import './BlackBar.scss';

function BlackBar(props) {
    return (
        <Container className="black-bar" fluid>
            
        </Container>
    );
}

export default BlackBar;